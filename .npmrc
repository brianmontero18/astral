# ─────────────────────────────────────────────────────────────────────
# Astral is a personal project. Use ONLY the public npm registry.
# Do NOT add scoped registries pointing to internal/private mirrors.
# Run `npm run check:registry` before commits if you suspect contamination.
# ─────────────────────────────────────────────────────────────────────
registry=https://registry.npmjs.org/
