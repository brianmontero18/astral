{
  "name": "astral-guide",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "start": "cd backend && npm start",
    "seed:local": "cd backend && npm run seed:local",
    "test": "cd backend && npm test",
    "check": "cd frontend && npx tsc --noEmit && cd ../backend && npx tsc --noEmit",
    "check:docs": "node scripts/check-docs-drift.mjs",
    "check:registry": "npm run registry:check",
    "registry:check": "node scripts/check-registry.mjs",
    "test:e2e": "playwright test --config=e2e/playwright.config.ts",
    "test:e2e:ui": "playwright test --config=e2e/playwright.config.ts --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "concurrently": "^9.1.2"
  }
}
