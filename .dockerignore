# Dependencies — reinstalled fresh inside each stage
**/node_modules

# Build artifacts — regenerated by the build stage
backend/dist
frontend/dist

# Local env & secrets — Render injects real values at runtime
**/.env

# Local databases & logs
*.db
*.sqlite
**/*.log

# VCS & tooling — not needed inside the image
.git
.claude
e2e
